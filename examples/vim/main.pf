# vim: set ft=hcl:

package "installed" "vim" {
  name = "vim"
  update = true
}

file "exists" "vimrc" {
  path = "${path.home}/.vimrc"
  source = "${path.module}/vimrc"

  attrs {
    owner = "chrism"
    group = "staff"
    perrmissions = 777
  }

  # Not Implemented yet...
  depends_on = ["${state.package.installed.vim}"]
}

execute "install_vim_plugins" {
  command = "echo install stuff here"
}
